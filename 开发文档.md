# Desktop App Update Server 构建文档 (Go)

## 1️⃣ 项目定位

**目标**：

- 集中管理多个桌面应用的版本和安装包。
- 支持 Electron、Go、Python、Qt、.NET 等多种桌面客户端。
- 支持 Windows、macOS、Linux 多平台。
- 自动生成客户端可识别的更新元数据：
  - Electron → `latest.yml` + `.blockmap`
  - 非 Electron → `update.json`
  - macOS 原生 → `appcast.xml`
  - Windows 原生 → 可选 `appcast.xml` 或自定义更新 JSON

- 提供统一 API，让客户端查询最新版本、下载包和更新日志。
- 可扩展灰度发布、强制更新、多渠道管理。

---

## 2️⃣ 技术选型

- **后端**：Go 1.21+（或最新稳定版本）
- **Web 框架**：Gin / Fiber / Chi
- **数据库**：PostgreSQL（管理应用元数据）
- **对象存储**：MinIO / AWS S3 / OSS（存放安装包）
- **认证**：JWT + API Key（可选）
- **部署**：Docker + Nginx 反向代理
- **自动更新元数据生成**：
  - `latest.yml`（Electron）
  - `update.json`（通用）
  - `appcast.xml`（macOS / Windows）

---

## 3️⃣ 数据库设计

### 3.1 `apps` 表

| 字段        | 类型      | 说明     |
| ----------- | --------- | -------- |
| id          | UUID      | 主键     |
| name        | VARCHAR   | 应用名称 |
| slug        | VARCHAR   | 唯一标识 |
| description | TEXT      | 应用描述 |
| icon_url    | VARCHAR   | 图标 URL |
| created_at  | TIMESTAMP | 创建时间 |

### 3.2 `releases` 表

| 字段            | 类型      | 说明                     |
| --------------- | --------- | ------------------------ |
| id              | UUID      | 主键                     |
| app_id          | UUID      | 所属应用                 |
| version         | VARCHAR   | 版本号                   |
| channel         | VARCHAR   | stable / beta / internal |
| changelog       | TEXT      | 发布日志                 |
| is_forced       | BOOL      | 是否强制更新             |
| staging_percent | INT       | 灰度发布比例（0-100）    |
| published_at    | TIMESTAMP | 发布时间                 |

### 3.3 `artifacts` 表

| 字段       | 类型      | 说明                             |
| ---------- | --------- | -------------------------------- |
| id         | UUID      | 主键                             |
| release_id | UUID      | 所属版本                         |
| platform   | VARCHAR   | windows / macos / linux          |
| arch       | VARCHAR   | x64 / arm64 / universal          |
| file_type  | VARCHAR   | exe / dmg / msi / zip / AppImage |
| file_url   | VARCHAR   | 下载地址                         |
| file_size  | BIGINT    | 文件大小                         |
| sha512     | VARCHAR   | 文件校验值                       |
| created_at | TIMESTAMP | 上传时间                         |

---

## 4️⃣ 核心功能模块

1. **应用管理**
   - 创建 / 编辑 / 删除应用
   - 设置描述、图标、默认渠道

2. **版本管理**
   - 上传新版本
   - 设置版本号、渠道、更新日志
   - 支持灰度/强制更新
   - 支持多平台、多架构安装包

3. **安装包管理**
   - 上传到对象存储
   - 自动生成 SHA512 校验值
   - 自动生成更新元数据：
     - Electron → `latest.yml` + `.blockmap`
     - 通用 → `update.json`
     - macOS → `appcast.xml`
     - Windows → 可选 `appcast.xml` 或 `update.json`

4. **更新 API**
   - 客户端获取最新版本信息
   - 返回下载 URL、SHA 校验值、安装包大小
   - 支持多平台自动选择

5. **安全与权限**
   - Admin 用户管理应用和版本
   - 客户端访问需要 token 验证（可选）

6. **统计与监控**
   - 下载统计
   - 更新成功/失败率
   - 版本使用量分析

---

## 5️⃣ API 接口示例

| 功能               | 接口                                  | 方法 | 描述                               |
| ------------------ | ------------------------------------- | ---- | ---------------------------------- |
| 获取最新版本元数据 | `/api/latest/:appSlug`                | GET  | 返回 `latest.yml` 或 `update.json` |
| 上传新版本         | `/api/apps/:appId/releases`           | POST | 上传版本信息 + 安装包              |
| 查询历史版本       | `/api/apps/:appId/releases`           | GET  | 获取版本列表                       |
| 获取安装包信息     | `/api/artifacts/:artifactId`          | GET  | 返回下载 URL + SHA + 文件大小      |
| 文件下载           | `/api/artifacts/:artifactId/download` | GET  | 返回安装包文件                     |

---

## 6️⃣ 自动生成更新元数据流程

1. 接收安装包上传
2. 读取版本号、平台、架构信息
3. 上传安装包到对象存储
4. 自动生成元数据：
   - Electron → `latest.yml` + `.blockmap`
   - 通用 → `update.json`
   - macOS → `appcast.xml`

5. 保存元数据到对象存储
6. 更新数据库最新版本信息
7. 客户端可以访问更新 API 获取最新元数据

---

## 7️⃣ 客户端接入示例

### Electron 客户端

```ts
import { autoUpdater } from "electron-updater";

autoUpdater.setFeedURL({
  provider: "generic",
  url: "https://updates.example.com/downloads/myapp/latest.yml",
});

autoUpdater.checkForUpdates();
```

### 非 Electron 客户端

- 请求 `update.json`
- 比较本地版本
- 下载新包
- 校验 SHA512
- 执行安装

---

## 8️⃣ 部署建议

- **后端服务**：Go + Gin/Fiber/Chi，Docker 部署
- **对象存储**：MinIO / S3 / OSS，保证安装包可访问
- **反向代理**：Nginx 提供 HTTPS
- **CI/CD**：
  - 自动构建应用
  - 自动上传安装包
  - 自动生成元数据

- **监控**：
  - Prometheus / Grafana
  - 下载次数统计
  - 异常告警
